apiVersion: v1
kind: Pod
metadata:
  name: test-alpine-pod
  namespace: default
  labels:
    app: test-app  # 与 BehaviorStub 中的 targetSelector 匹配
spec:
  containers:
  - name: test-container
    image: alpine:3.18
    command:
    - /bin/sh
    - -c
    - sleep 3600
  restartPolicy: Always
