Your k8s-http-fake-operator has been deployed successfully!

To get started:

1. Check the deployment status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "k8s-http-fake-operator.name" . }}

2. View the service:
   kubectl get svc {{ include "k8s-http-fake-operator.fullname" . }} -n {{ .Release.Namespace }}

3. Create a sample BehaviorStub:
   kubectl apply -f config/samples/httpteststub.example.com_v1_httpteststub.yaml

4. Check the BehaviorStub status:
   kubectl get behaviorstubs -n {{ .Release.Namespace }}

5. View operator logs:
   kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "k8s-http-fake-operator.name" . }}

Note: The MutatingWebhookConfiguration will be automatically created when you create the first BehaviorStub,
and will be automatically deleted when you delete the last BehaviorStub.

For more information, visit: https://github.com/gfhw/k8s-http-fake-operator
